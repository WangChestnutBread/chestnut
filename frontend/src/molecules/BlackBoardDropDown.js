import TabDropItem from "../atoms/TabDropItem";

function BlackBoardDropDown({ chapterTitle }) {

  return (
    <div>
      {/* {chapterTitle.map((item, i) => {
        return <TabDropItem menu={""} handleOnClick={""} />;
      })} */}
    </div>
  );
}

export default BlackBoardDropDown;
